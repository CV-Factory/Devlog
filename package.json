{
  "name": "devlog-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Devlog: Agentic Conversation to Blog Pipeline",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test",
    "lint": "biome check packages sst.config.ts package.json tsconfig.json knip.json .dependency-cruiser.js biome.json",
    "lint:fix": "biome check --write packages sst.config.ts package.json tsconfig.json knip.json .dependency-cruiser.js biome.json",
    "type-check": "bun --filter '*' type-check",
    "knip": "knip",
    "depcruise": "depcruise packages"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "^1.3.8",
    "dependency-cruiser": "^17.3.7",
    "knip": "^5.83.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "sst": "^3.17.38"
  }
}
